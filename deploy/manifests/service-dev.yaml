apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: hello-canary-dev
spec:
  template:
    spec:
      serviceAccountName: hello-canary-sa@humblebundle-stg.iam.gserviceaccount.com
      containers:
        - image: hello-app # placeholder — replaced at release time
          env:
            - name: APP_ENV
              value: dev
